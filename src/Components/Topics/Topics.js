import React, { Component } from 'react';
class Topics extends Component {
    render() {
        // const {topics} = this.props
        // const topicsList = topics.length === 0 ? (<div>加载中...</div>) :
        // (<ul>{topics.map(({id ,title})=>{<li key={id}>{title}</li>})}</ul>)
        return (
            <div>
            </div>
        );
    }
}

export default Topics;